# Backend for Wol-API

## Running the server

- Must be in the mono-repo root directory
- Run `docker compose up --build`

## Running the seeder

- Must be in the backend directory
- Run `cargo seed`
